name: Dynare 5.5 Octave 6.4.0

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  dynare_5_5_octave_6_4_0:
    uses: ./.github/workflows/dynare-testsuite-common.yml
    with:
      dynare-version: "5.5"
      testsuite: "octave"
    secrets:
      matlab-licensefile-base64: ${{ secrets.MATLAB_LICENSEFILE_BASE64 }}